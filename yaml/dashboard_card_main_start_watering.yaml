# Preferred Start Watering control - real ON/OFF toggle.
# ON while phase is first_half / pause / second_half; OFF when idle.
# Turn ON -> start sequence; turn OFF -> stop sequence.
# Add as Tile (with toggle) on Garden or Newbury.

type: tile
entity: switch.water_garden_watering
name: Start Watering
features:
  - type: toggle
