- id: '1784746590314'
  alias: Water Garden Start
  description: >-
    Starts the split-run sequence only from Start Watering (button/script).
    Pump toggle alone does not start the sequence.
  mode: single
  triggers:
    - trigger: state
      entity_id: input_button.water_garden_start
  conditions:
    - condition: state
      entity_id: input_select.water_garden_phase
      state: idle
  actions:
    - action: script.water_garden_start
      data: {}
